extends layout

block content
  .grid.portfoliogrid
    each album in albums
      article.hentry
        header.entry-header
          .entry-thumbnail
            a(href='/album/' + album)
              img.attachment-post-thumbnail.size-post-thumbnail.wp-post-image(src='https://37.220.86.212:443/images/' + album + '/cover/cover.jpg', alt='p1')
            if isAuthor
              button.delete-album(type="button" value=album) Удалить